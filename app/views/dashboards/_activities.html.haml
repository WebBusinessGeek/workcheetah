%h1 Activity Feed
%ul
- if @activities
  - @activities.each do |activity|
    %li{"id" => dom_id(activity)}
      = link_to [activity], method: :delete, remote: true do
        %span.glyphicon.glyphicon-trash
      = render_activity activity
.buttonholds
  %a.btn.btn-viewall{ :href => activities_path} View All
