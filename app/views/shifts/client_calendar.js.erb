$('#index').html('<%= j render("shifts/client_calendar") %>')
$('#client_calendar').fullCalendar({
    defaultView: 'agendaWeek',
    events: $('#client_calendar').data('shifts'),
    eventClick: function(calEvent, jsEvent, view){
        jsEvent.preventDefault()
    }
})