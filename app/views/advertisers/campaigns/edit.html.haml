.container
  .row
    .span10
      %h3 Edit Campaign
      = simple_form_for @campaign, url: advertisers_campaign_path(@campaign) do |f|
        = render 'form', f: f
        .span2
        = f.submit class: "btn"