Someone new has applied for your job.

Click the link below to preview their resume and add them as recruit if you'd like:
<%= job_job_applications_url(@job_application.job, @job_application) %>

----------------
WorkCheetah Team