.tweets_show
	.white_box
		%p
			%b Body:
			= @tweet.body
		%p
			%b For accounts:
			= @tweet.for_accounts
		%p
			%b For public:
			= @tweet.for_public
		%p
			%b For resumes:
			= @tweet.for_resumes
		= link_to 'Edit', edit_tweet_path(@tweet)
		|
		\#{link_to 'Back', tweets_path}
