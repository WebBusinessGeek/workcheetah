.articles_index.container
  %h2#careerstuff Career Stuff 101
  %h3#careersub Powered by WorkCheetah.com
  - if @articles.any?
    - @articles.each do |article|
      %h4.title= link_to article.title, article
      .teaser= ActionView::Base.full_sanitizer.sanitize(article.body).truncate(200)
      = link_to "Show more", article, class: "show_more"
  - else
    %p No articles added yet.