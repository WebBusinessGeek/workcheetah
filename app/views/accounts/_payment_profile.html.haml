- if @account.payment_profiles.any?
  - @account.payment_profiles.each do |pp|
    - if pp.expiration
      %strong Expires:
      = pp.expiration
    - if pp.cc_number_preview
      %strong Last 4 Numbers:
      = pp.cc_number_preview
    - if pp.created_at
      %strong Created on:
      = pp.created_at.strftime("%m/%d/%Y")
    = link_to "Delete this form of payment", payment_profile_path(pp), method: :delete, confirm: "Are you sure you want to remove this payment profile?"
    %br/
- else
  %p
    Looks like you don't have any payment profiles setup yet. Use the menu on the left to add one.