console.log("<%= @confirmation.inspect %>");
$('#myModal .modal-body').html('<%= j render partial: "confirmations/new", confirmation: @confirmation %>');
$('#myModal').modal("show").css({
    width: 'auto',
    'margin-left': function () {
        return -($(this).width() / 2);
    }
});